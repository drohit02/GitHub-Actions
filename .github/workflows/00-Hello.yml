# Hello World GitHub Actions Workflow

name: "👋 [HelloWorld-Workflow] Hello World GitHub Actions"

run-name: "👋 [HelloWorld] ${{ github.actor }} → Saying Hello to GitHub Actions"

on:

  push:

jobs:

  hello-job:

    runs-on: ubuntu-latest

    steps:

      - name: Say Hello via GitHub Actions

        run: echo "Hello World, This is GitHub Actions Workflow!!"